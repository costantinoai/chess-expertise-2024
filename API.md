Chess Expertise 2024 — API Reference

This file is generated by `scripts/generate_api.py`. Do not edit manually.


## behavioural

- Python Modules:
  - `behavioural/bh_fmri.py`
    - function `analyze_group(df_group, expertise_label, df_cat)` — Complete analysis pipeline for a single group (Experts or Novices): (behavioural/bh_fmri.py:554)
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri.py:398)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri.py:374)
    - function `correlate_model_behavior(d_group, df_cat, expertise_label)` — For each categorical (and scalar) column in df_cat, compute: (behavioural/bh_fmri.py:500)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri.py:338)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri.py:317)
    - function `load_stimulus_categories(cat_path)` — Load stimulus categories from an Excel file and filter valid stim_id range. (behavioural/bh_fmri.py:486)
    - function `plot_choice_frequency(data, expertise_label)` — Create a bar plot showing how often each stimulus (by ID) was chosen in pairwise comparisons. (behavioural/bh_fmri.py:144)
    - function `plot_mds(dissimilarity_matrix, bold_title, expertise_label)` — Perform Multidimensional Scaling (MDS) on a precomputed dissimilarity matrix and plot the 2D embedding. (behavioural/bh_fmri.py:117)
    - function `plot_model_behavior_correlations(expert_results, novice_results, column_labels)` — Generate a side-by-side barplot of bootstrapped Pearson correlations (with 95% CI) (behavioural/bh_fmri.py:184)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Delegate RDM heatmap plotting to shared behavioural plotting module. (behavioural/bh_fmri.py:99)
    - function `plot_shared_colorbar(cmap, vmin, vmax)` — Delegate to shared plotting module for colorbar. (behavioural/bh_fmri.py:94)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri.py:422)
    - function `set_output_dir(out_dir)` —  (behavioural/bh_fmri.py:56)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri.py:466)
  - `behavioural/bh_fmri_intercorr.py`
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri_intercorr.py:199)
    - function `compute_reliabilities_from_trials(trial_df_dict)` — Compute within-group and between-group RDM reliability from raw trial data (not precomputed RDMs). (behavioural/bh_fmri_intercorr.py:340)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri_intercorr.py:175)
    - function `correlate_model_behavior(d_group, df_cat, expertise_label)` — For each categorical (and scalar) column in df_cat, compute: (behavioural/bh_fmri_intercorr.py:287)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri_intercorr.py:139)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri_intercorr.py:118)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Delegate to shared RDM heatmap plotter (no saving here). (behavioural/bh_fmri_intercorr.py:88)
    - function `plot_reliability_bars(plot_data, out_fig, out_csv, run_id)` — Plot within- and between-group RDM reliability for Experts and Novices, (behavioural/bh_fmri_intercorr.py:421)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri_intercorr.py:223)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri_intercorr.py:267)
  - `behavioural/bh_fmri_splithalf.py`
    - function `_between_worker(args)` — Compute a split-half RDM similarity score between two groups using random half-sampling. (behavioural/bh_fmri_splithalf.py:73)
    - function `_split_half_worker(args)` — Compute a split-half RDM reliability score for a single group using a random split of subjects. (behavioural/bh_fmri_splithalf.py:29)
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri_splithalf.py:295)
    - function `compute_reliabilities_from_trials(trial_df_dict, n_iter, random_seed)` — Estimate within- and between-group reliability of behavioral RDMs using split-half correlations. (behavioural/bh_fmri_splithalf.py:389)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri_splithalf.py:271)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri_splithalf.py:235)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri_splithalf.py:214)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Delegate to shared heatmap plotter. (behavioural/bh_fmri_splithalf.py:184)
    - function `plot_reliability_bars(plot_data, out_fig, out_csv, run_id)` — Plot within- and between-group RDM reliability for Experts and Novices, (behavioural/bh_fmri_splithalf.py:473)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri_splithalf.py:319)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri_splithalf.py:363)
  - `behavioural/modules/chess.py`
    - function `check_accuracy(sub_resp, recommended_moves)` —  (behavioural/modules/chess.py:13)
    - function `clean_chess_move(move)` —  (behavioural/modules/chess.py:43)
    - function `convert_shorthand_to_long(fen, shorthand_moves, stim_id)` — Convert shorthand chess moves into from-cell-to-cell algebraic notation using a given FEN. (behavioural/modules/chess.py:22)
    - function `convert_single_shorthand(fen, shorthand_move, stim_id)` — Convert a shorthand chess move into from-cell-to-cell algebraic notation using a given FEN. (behavioural/modules/chess.py:51)
    - function `get_all_moves_with_int_eval(moves, evals)` —  (behavioural/modules/chess.py:17)
  - `behavioural/modules/helpers.py`
    - function `calculate_mean_and_ci(data)` —  (behavioural/modules/helpers.py:21)
    - function `load_and_sort_data(dataset_path, net_responses_path, humans_response_path)` — Load the datasets into Pandas DataFrames and sort them by 'stim_id'. (behavioural/modules/helpers.py:27)
- MATLAB Files: none found

## common

- Python Modules:
  - `common/behavioural_plotting.py`
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, strategies, strat_colors, strat_alphas, colormap, vmin, vmax)` — Plot an RDM as a heatmap with strategy group bars along axes. (common/behavioural_plotting.py:33)
    - function `plot_shared_colorbar(cmap, vmin, vmax, out_dir, filename)` — Render a standalone colorbar image with given colormap and limits. (common/behavioural_plotting.py:14)
  - `common/behavioural_utils.py`
    - function `compute_strategy_colors_alphas(strategies, col_green, col_red)` — Assign colors and alphas per strategy block. (common/behavioural_utils.py:9)
  - `common/brain_plotting.py`
    - function `plot_glass_brain_from_df(df_input, atlas_img, title, cmap, base_font_size, out_dir)` —  (common/brain_plotting.py:49)
    - function `plot_surface_map_flat(img, title, threshold, output_file, cmap)` —  (common/brain_plotting.py:23)
    - function `save_and_open_plotly_figure(fig, title, outdir, png_out)` —  (common/brain_plotting.py:13)
  - `common/common_utils.py`
    - function `add_file_logger(log_file, level)` — Attach a file handler to the root logger in addition to console output. (common/common_utils.py:24)
    - function `create_output_directory(directory_path)` — Create an output directory if it does not exist. (common/common_utils.py:56)
    - function `create_run_id()` — Return a timestamp string YYYYMMDD-HHMMSS for naming outputs. (common/common_utils.py:51)
    - function `save_script_to_file(output_directory)` — Copy the calling script to the output directory for provenance. (common/common_utils.py:67)
    - function `set_rnd_seed(seed)` — Set random seeds (NumPy and Python) for reproducibility. (common/common_utils.py:45)
  - `common/logging_utils.py`
    - function `setup_logging(log_file, level)` — Configure and return a logger with a consistent format. (common/logging_utils.py:5)
  - `common/niimg_utils.py`
    - function `build_design_matrix(n_exp, n_nov)` —  (common/niimg_utils.py:31)
    - function `find_nifti_files(data_dir, pattern)` —  (common/niimg_utils.py:18)
    - function `fisher_z_maps(file_list)` —  (common/niimg_utils.py:27)
    - function `load_and_mask_imgs(imgs_list, mask_img, mask_func)` — Apply a brain mask to a list of NIfTI images. (common/niimg_utils.py:37)
  - `common/plotting_utils.py`
    - function `plot_mvpa_barplot(data, x_hue, y_col, x_groups, chance_level, title, hue_order, x_groups_order, out_dir, plot_points, use_corrected_p, vmin, vmax)` — Generic barplot for MVPA group results with optional significance stars. (common/plotting_utils.py:15)
  - `common/stats_plotting.py`
    - function `plot_correlations(df_pos, df_neg, df_diff, out_fig, out_csv, run_id, col_pos, col_neg, palette)` — Paired bar plot (POS vs NEG), with optional CI and FDR stars if present. (common/stats_plotting.py:14)
    - function `plot_difference(diff_df, out_fig, run_id, col_pos, col_neg)` — Plot bar chart of correlation differences (pos - neg). Supports CI and FDR stars if provided. (common/stats_plotting.py:71)
  - `common/stats_utils.py`
    - function `corr_diff_bootstrap(term_map, x, y, n_boot, ci_alpha, n_jobs, rng)` — Bootstrap CI and p-value for difference in Pearson correlations r(term,x) - r(term,y). (common/stats_utils.py:95)
    - function `fdr_correction(p_values, alpha, method)` — Multiple-comparison correction; returns (reject_mask, corrected_pvalues). (common/stats_utils.py:73)
    - function `independent_ttest(x, y, equal_var, alternative)` — Independent-samples t-test (Welch by default) with 95% CI on mean difference. (common/stats_utils.py:47)
    - function `mean_ci_t(data, confidence)` — Mean and t-based confidence interval (two-sided). (common/stats_utils.py:136)
    - function `one_sample_ttest(values, mu, alternative)` — One-sample t-test against mean `mu` with 95% CI. (common/stats_utils.py:20)
    - function `pearson_corr_bootstrap(x, y, n_boot, ci)` — Pearson correlation with bootstrap CI via Pingouin. (common/stats_utils.py:83)
    - function `spearman_corr(x, y)` — Spearman correlation r and p-value. (common/stats_utils.py:130)
  - `common/surface_plotting.py`
    - function `plot_left_fsaverage_grid(overlay_lh, label_map_lh, title, out_path, color_range, cmap_positive, cmap_negative, add_colorbar, add_contours, contour_color, contour_linewidth)` — Plot a 2x3 grid of fsaverage LH views with optional ROI contours. (common/surface_plotting.py:20)
- MATLAB Files: none found

## manifold

- Python Modules:
  - `manifold/modules/pr_core.py`
    - class `Config` —  (manifold/modules/pr_core.py:28)
    - function `_assert_file_exists(path, label)` —  (manifold/modules/pr_core.py:43)
    - function `_spm_dir_from_swd(spm_obj, fallback)` —  (manifold/modules/pr_core.py:48)
    - function `compute_pr(roi_data)` —  (manifold/modules/pr_core.py:94)
    - function `consolidate_results(expert_pr, novice_pr, roi_labels, stats_df, roi_name_map)` —  (manifold/modules/pr_core.py:126)
    - function `get_spm_condition_betas(subject_id, cfg)` —  (manifold/modules/pr_core.py:53)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois, cfg)` —  (manifold/modules/pr_core.py:83)
    - function `main(cfg)` —  (manifold/modules/pr_core.py:138)
    - function `per_roi_welch_and_fdr(expert_pr, novice_pr, roi_labels, alpha)` —  (manifold/modules/pr_core.py:113)
    - function `process_subject(subject_id, atlas_data, unique_rois, cfg)` —  (manifold/modules/pr_core.py:103)
  - `manifold/modules/pr_utils.py`
    - function `compute_entropy_pr(roi_data)` —  (manifold/modules/pr_utils.py:62)
    - function `get_spm_info(subject_id)` —  (manifold/modules/pr_utils.py:20)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois)` —  (manifold/modules/pr_utils.py:48)
    - function `plot_voxelcount_vs_pr(df, y_col, title_prefix, fname, out_dir)` —  (manifold/modules/pr_utils.py:82)
    - function `process_subject(subject_id, atlas_data, unique_rois)` —  (manifold/modules/pr_utils.py:72)
  - `manifold/plot_pr_roi_size.py`
    - function `main()` —  (manifold/plot_pr_roi_size.py:37)
- MATLAB Files: none found

## mvpa

- Python Modules:
  - `mvpa/manifold.py`
    - function `compute_entropy_pr_per_roi(roi_data)` — Computes the participation ratio (PR) for a given ROI data matrix. (mvpa/manifold.py:262)
    - function `fdr_ttest(group1_vals, group2_vals, roi_labels, alpha)` — Performs independent two-sample t-tests for each ROI, then applies FDR correction. (mvpa/manifold.py:302)
    - function `get_spm_betas_info(subject_id)` — Returns, for each condition label (e.g. 'C1', 'C2', …), a list of dicts: (mvpa/manifold.py:78)
    - function `get_spm_info(subject_id)` — Loads the subject's SPM.mat structure and returns averaged beta images. (mvpa/manifold.py:126)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois)` — Loads and extracts ROI-level voxel data from averaged beta images for one subject. (mvpa/manifold.py:210)
    - function `main()` — Main analysis pipeline: (mvpa/manifold.py:601)
    - function `plot_all_rois_with_significance(stat_df, measure_name, group_means_df, expert_vals, nonexpert_vals, output_dir, use_fdr, sort_by, custom_colors)` — Plots ROI results for a given measure (e.g., Participation Ratio), (mvpa/manifold.py:356)
    - function `process_subject(subject_id, atlas_data, unique_rois)` — Pipeline for processing a single subject: (mvpa/manifold.py:557)
  - `mvpa/modules/helpers.py`
    - function `filter_significant_ROIs(sliced_dict, use_corrected_p_values, P_ALPHA, FDR_ALPHA)` — Updates the 'mean' column in the dataframes within the given dictionary based on thresholding rules (mvpa/modules/helpers.py:119)
    - function `load_all_data(root_path, expert_subjects, nonexpert_subjects, level)` — Load data for expert & non-expert subjects, concatenate into one DataFrame. (mvpa/modules/helpers.py:96)
    - function `load_subject_data(root_path, subject_list, is_expert, level)` — Load decoding accuracies from TSV files for a set of subjects, producing (mvpa/modules/helpers.py:22)
  - `mvpa/modules/plotting_helpers.py`
    - function `plot_group_level_mvpa(ttest_results_path, plot_corrected)` — Plot MVPA group-level results across different levels and analyses, using (mvpa/modules/plotting_helpers.py:27)
    - function `plot_mvpa_barplot()` — Thin wrapper that delegates MVPA bar plotting to `common.plotting_utils.plot_mvpa_barplot`. (mvpa/modules/plotting_helpers.py:178)
  - `mvpa/modules/surf_helpers.py`
    - function `build_significance_overlay(stats_df, lh_labels, rh_labels)` — Construct per-hemisphere overlays based on significant ROI statistics. (mvpa/modules/surf_helpers.py:135)
    - function `plot_experts_vs_non_experts_grid(figures_dict, title, out_path)` — Arrange saved brain-plot images in a 2×N grid (Novices top, Experts bottom). (mvpa/modules/surf_helpers.py:235)
    - function `plot_fsaverage_overlay(overlays, title, out_path, color_range, cmap_positive, cmap_negative, views)` — Delegate fsaverage left-hemisphere grid plotting to shared helper. (mvpa/modules/surf_helpers.py:208)
    - function `plot_significant_surface_overlays(ttest_results_path, select_corrected_p_values, LH_ANNOT, RH_ANNOT, P_ALPHA, FDR_ALPHA, out_root)` — Load group results, build ROI/cortex overlays, and save fsaverage plots. (mvpa/modules/surf_helpers.py:43)
  - `mvpa/modules/text_utils.py`
    - function `format_contrast(s)` — Normalize contrast labels for display. (mvpa/modules/text_utils.py:3)
  - `mvpa/mvpa_barplot_comparison.py`
    - function `build_regressor_dataframe(regressor, analysis_results, roi_df, chance_value)` — Construct a long-format DataFrame for a single regressor, combining novices and experts stats, (mvpa/mvpa_barplot_comparison.py:145)
    - function `find_single_file(directory, pattern, file_description)` — Search for a single file matching the given glob pattern in the specified directory. (mvpa/mvpa_barplot_comparison.py:92)
    - function `load_analysis_results(pickle_path)` — Unpickle and return the analysis results dictionary. (mvpa/mvpa_barplot_comparison.py:131)
    - function `load_roi_annotations(roi_annotation_path)` — Load ROI annotation data from a single TSV file inside the given directory. (mvpa/mvpa_barplot_comparison.py:116)
    - function `plot_expert_vs_novice(df, analysis_type, regressor_key, regressor_mapping, output_dir, chance_value, plot_nonsig_labels)` — Create and save a side-by-side bar plot comparing Experts vs Novices across ROIs, (mvpa/mvpa_barplot_comparison.py:259)
  - `mvpa/mvpa_ttest.py`
    - function `load_mvpa_subject(filepath)` — Loads a single subject's MVPA .tsv file and appends 'subject' + 'expert' metadata. (mvpa/mvpa_ttest.py:46)
    - function `main(mvpa_roots, mvpa_subdirs)` —  (mvpa/mvpa_ttest.py:300)
    - function `organize_data_by_target_and_expertise(df)` — Organizes a flat dataframe into a nested dict: (mvpa/mvpa_ttest.py:17)
    - function `run_stats_on_df(df, method)` — Runs expert vs. nonexpert stats for all regressors in the given DataFrame. (mvpa/mvpa_ttest.py:87)
  - `mvpa/plot_surf_glasser.py`
    - function `load_freesurfer_lut(file_path)` — Load a FreeSurfer-style LUT file and create a Matplotlib colormap. (mvpa/plot_surf_glasser.py:16)
  - `mvpa/print_mvpa_stats.py`
    - function `extract_stats(result_dict, regressor)` —  (mvpa/print_mvpa_stats.py:55)
    - function `find_single_file(directory, pattern)` —  (mvpa/print_mvpa_stats.py:45)
    - function `load_results(pkl_path)` —  (mvpa/print_mvpa_stats.py:51)
    - function `print_latex_table(df, analysis, regressor, label)` —  (mvpa/print_mvpa_stats.py:76)
  - `mvpa/test_number_of_runs.py`
    - function `cohens_d_average_sd_unequal_var(x, y)` — Cohen's d using the average SD (often denoted d_av), recommended when variances differ. (mvpa/test_number_of_runs.py:180)
    - function `cohens_d_pooled_equal_var(x, y)` — Cohen's d using pooled SD (appropriate when variances are approximately equal). (mvpa/test_number_of_runs.py:150)
    - function `count_runs_from_spm(spm_path)` — Infer the number of runs for a subject from SPM.mat. (mvpa/test_number_of_runs.py:67)
    - function `hedges_g_from_d(d, nx, ny)` — Hedges' g bias correction applied to any Cohen's d-like estimator. (mvpa/test_number_of_runs.py:208)
    - function `main()` — Orchestrate data loading, testing, and reporting. (mvpa/test_number_of_runs.py:367)
    - function `save_run_count_latex_table(s_exp, s_nov, lev_stat, lev_p, test_name, t_stat, df_used, p_value, ci_low, ci_high, d_label, d_value, g_value, mw_stat, mw_p, output_dir, filename)` — Save a multi-column LaTeX table summarizing the analysis. (mvpa/test_number_of_runs.py:293)
    - function `save_run_count_report_txt(s_exp, s_nov, test_name, t_stat, df_used, p_value, ci_low, ci_high, d_label, d_value, g_value, lev_stat, lev_p, mw_stat, mw_p, output_dir, filename)` — Save a plain-text summary report of the analysis. (mvpa/test_number_of_runs.py:235)
    - function `summarize(arr)` — Compute basic NaN-safe descriptive stats for a 1D array. (mvpa/test_number_of_runs.py:122)
  - `mvpa/utils/sanitize_json.py`
    - function `find_nan_json_files(folder)` — Scan for JSON files in the directory that contain non-compliant float values. (mvpa/utils/sanitize_json.py:51)
    - function `is_nan(value)` — Check if a value is NaN. (mvpa/utils/sanitize_json.py:16)
    - function `main(folder, dry_run)` —  (mvpa/utils/sanitize_json.py:72)
    - function `process_file(file_path, dry_run)` — Process a single JSON file, replacing NaN values and optionally printing changes. (mvpa/utils/sanitize_json.py:20)
  - `mvpa/utils/svm_class_displacement.py`
    - function `load_eye_tracking_data(root_dir, participants)` — Load and preprocess eye tracking data from the specified directory for the given participants. (mvpa/utils/svm_class_displacement.py:50)
    - function `main(eye_tracking_data)` — Main function to run the SVM decoding analysis. (mvpa/utils/svm_class_displacement.py:330)
    - function `plot_results(results)` — Create and display publication-ready visualizations of the SVM decoding results. (mvpa/utils/svm_class_displacement.py:220)
    - function `prepare_features(eye_tracking_data)` — Prepare features for SVM classification from eye-tracking data. (mvpa/utils/svm_class_displacement.py:103)
    - function `run_svm_decoding(X, y, groups)` — Perform SVM decoding analysis using stratified group k-fold cross-validation. (mvpa/utils/svm_class_displacement.py:139)
  - `mvpa/utils/svm_class_xy.py`
    - function `load_eye_tracking_data(root_dir, participants)` — Load and preprocess eye tracking data from the specified directory for the given participants. (mvpa/utils/svm_class_xy.py:42)
    - function `main(eye_tracking_data)` — Main function to run the SVM decoding analysis. (mvpa/utils/svm_class_xy.py:322)
    - function `plot_results(results)` — Create and display publication-ready visualizations of the SVM decoding results. (mvpa/utils/svm_class_xy.py:212)
    - function `prepare_features(eye_tracking_data)` — Prepare features for SVM classification from eye-tracking data. (mvpa/utils/svm_class_xy.py:95)
    - function `run_svm_decoding(X, y, groups)` — Perform SVM decoding analysis using stratified group k-fold cross-validation. (mvpa/utils/svm_class_xy.py:131)
- MATLAB Files:
  - `mvpa/matlab_helpers/createTimestamp.m`
  - `mvpa/matlab_helpers/expand_fmriprep_surfaces.m`
  - `mvpa/matlab_helpers/generate_con_img.m`
  - `mvpa/matlab_helpers/mvpa/ROI.m`
  - `mvpa/matlab_helpers/mvpa/ROIManager.m`
  - `mvpa/matlab_helpers/mvpa/getHierarchicalItems.m`
  - `mvpa/matlab_helpers/mvpa/loadAndIntersectROI.m`
  - `mvpa/matlab_helpers/mvpa/loadColorTable.m`
  - `mvpa/matlab_helpers/mvpa/performMVPA.m`
  - `mvpa/matlab_helpers/mvpa/performMVPA_matlabsvm.m`
  - `mvpa/matlab_helpers/mvpa/performRSACorrelation.m`
  - `mvpa/matlab_helpers/mvpa/performRSARegression.m`
  - `mvpa/matlab_helpers/mvpa/prepareRoiFile.m`
  - `mvpa/matlab_helpers/mvpa/sliceDatasetByHierarchy.m`
  - `mvpa/matlab_helpers/saveCurrentScript.m`
  - `mvpa/mvpa_rsa_all_regressors.m`
  - `mvpa/rsa_searchlight_final.m`
  - `mvpa/utils/make_sphere_rois.m`

## neurosynth

- Python Modules:
  - `neurosynth/modules/config.py`
    - function `setup_logging(log_file, level)` — Configure and return a logger with a consistent format. (neurosynth/modules/config.py:20)
  - `neurosynth/modules/io_utils.py`
    - function `load_nifti(path)` — Load an image file using nilearn and return the NIfTI image object. (neurosynth/modules/io_utils.py:23)
    - function `load_term_maps(map_dir)` — Return a dictionary mapping each term name to its NIfTI file. (neurosynth/modules/io_utils.py:9)
  - `neurosynth/modules/plot_utils.py`
    - function `plot_correlations(df_pos, df_neg, df_diff, out_fig, out_csv, run_id)` — Paired bar plot (POS vs NEG), delegated to shared plotting. (neurosynth/modules/plot_utils.py:49)
    - function `plot_difference(diff_df, out_fig, run_id, col_pos, col_neg)` — Plot bar chart of correlation differences (pos - neg), delegated to shared plotting. (neurosynth/modules/plot_utils.py:67)
    - function `plot_map(arr, ref_img, title, outpath, thresh)` — Delegate to shared brain plotting utility. (neurosynth/modules/plot_utils.py:19)
    - function `plot_term_maps(term_maps, out_dir)` — Plot and save brain maps for each term in term_maps. (neurosynth/modules/plot_utils.py:25)
  - `neurosynth/modules/rsa_main.py`
    - function `_masked(imgs_list)` —  (neurosynth/modules/rsa_main.py:47)
    - function `_split_by_group(files, expert_ids, novice_ids)` —  (neurosynth/modules/rsa_main.py:38)
    - function `main(data_dir, term_dir, results_root, smoothing_mm, min_voxel_value)` —  (neurosynth/modules/rsa_main.py:53)
  - `neurosynth/modules/stats_utils.py`
    - function `compute_all_zmap_correlations(z_pos, z_neg, term_maps, ref_img, n_boot, fdr_alpha, ci_alpha, random_state, n_jobs)` — Compute correlation statistics for positive and negative z-maps against (neurosynth/modules/stats_utils.py:137)
    - function `generate_latex_multicolumn_table(data_dict, output_path, table_type, caption, label)` — Generate and save a LaTeX multicolumn table from multiple regressors. (neurosynth/modules/stats_utils.py:366)
    - function `get_brain_mask(ref)` — Loads the ICBM152 GM brain mask and resamples it to match the reference image. (neurosynth/modules/stats_utils.py:23)
    - function `remove_useless_data(data, brain_mask_flat)` — Remove problematic voxels from stacked maps. (neurosynth/modules/stats_utils.py:82)
    - function `save_latex_correlation_tables(df_pos, df_neg, diff_df, run_id, out_dir)` — Save LaTeX tables for positive/negative/difference z-map correlations. (neurosynth/modules/stats_utils.py:280)
    - function `split_and_convert_t_to_z(t_map, dof)` — Take a 3D t-map and produce two 3D z-maps: (neurosynth/modules/stats_utils.py:53)
  - `neurosynth/modules/univariate_main.py`
    - function `main(term_dir, brain_dir, dof, min_voxel_value)` —  (neurosynth/modules/univariate_main.py:40)
  - `neurosynth/modules/univariate_utils.py`
    - function `bootstrap_corr(x, y, n_boot)` — Bootstrap Pearson correlation (delegates to common.stats_utils). (neurosynth/modules/univariate_utils.py:103)
    - function `bootstrap_corr_diff(term_map, x, y, n_boot, rng, ci_alpha, n_jobs)` — Proxy to common.stats_utils.corr_diff_bootstrap for convenience. (neurosynth/modules/univariate_utils.py:128)
    - function `build_design_matrix(n_exp, n_nov)` — Return DataFrame with intercept and group (+1 experts, -1 novices). (neurosynth/modules/univariate_utils.py:95)
    - function `extract_corr_results(result)` — Extract r, CI low/high, and p-value from Pingouin result DataFrame. (neurosynth/modules/univariate_utils.py:120)
    - function `find_nifti_files(data_dir, pattern)` — Recursively list .nii.gz under `data_dir`, optionally filtering by substring `pattern`. (neurosynth/modules/univariate_utils.py:168)
    - function `load_and_mask_imgs(file_list)` — Load NIfTI images and mask them with ICBM GM brain mask; returns (masked_imgs, brain_mask). (neurosynth/modules/univariate_utils.py:188)
    - function `plot_surface_map(img, title, threshold, output_file, cmap)` — Project volumetric image onto inflated cortical surfaces and build Plotly figure. (neurosynth/modules/univariate_utils.py:35)
    - function `plot_surface_map_flat(img, title, threshold, output_file, cmap)` — Project volumetric image onto flat cortical surfaces and build Plotly figure. (neurosynth/modules/univariate_utils.py:133)
    - function `save_and_open_plotly_figure(fig, title, outdir, png_out)` — Save Plotly figure to HTML and PNG. (neurosynth/modules/univariate_utils.py:18)
    - function `split_by_group(files, expert_ids, novice_ids)` — Split file paths into expert/novice by matching 'sub-<ID>' in filename. (neurosynth/modules/univariate_utils.py:178)
    - function `split_zmap_by_sign(z_map)` — Return (z_pos, z_neg) where z_neg is negative values kept negative. (neurosynth/modules/univariate_utils.py:210)
    - function `t_to_two_tailed_z(t_map, dof)` — Convert a t-map to a signed, two-tailed z-map (retain sign). (neurosynth/modules/univariate_utils.py:199)
- MATLAB Files: none found

## rois

- Python Modules:
  - `rois/io.py`
    - function `load_atlas_img(atlas_path)` — Load atlas labels image from path. (rois/io.py:21)
    - function `load_roi_annotations(set_name, tsv_path)` — Load ROI annotations either by known set name or explicit TSV path. (rois/io.py:26)
    - function `make_labels_masker(labels_img, standardize)` — Create a simple mean-extracting labels masker. (rois/io.py:35)
  - `rois/legacy/make_glasser_rois.py`
    - function `assign_color(cortex_id)` —  (rois/legacy/make_glasser_rois.py:100)
    - function `parse_afni_value_label_dtable(head_path)` —  (rois/legacy/make_glasser_rois.py:17)
    - function `regionName_to_cifti(region_name)` —  (rois/legacy/make_glasser_rois.py:53)
  - `rois/meta.py`
    - class `ROIInfo` —  (rois/meta.py:28)
    - function `_default_glasser_regions_info()` —  (rois/meta.py:140)
    - function `_load_region_info_tsv(tsv_path)` —  (rois/meta.py:37)
    - function `get_region_color(idx, set_name)` —  (rois/meta.py:225)
    - function `get_region_label(idx, set_name)` —  (rois/meta.py:220)
    - function `get_roi_info(set_name)` — Return ROIInfo for a given set, preferring TSV metadata if present. (rois/meta.py:207)
    - function `validate_roi_tsv(tsv_path, valid_fs_names)` — Validate a ROI TSV for required columns/values and optional fs_name matches. (rois/meta.py:95)
- MATLAB Files: none found
