Chess Expertise 2024 — API Reference

This file is generated by `scripts/generate_api.py`. Do not edit manually.


## behavioural

- Python Modules:
  - `behavioural/bh_fmri.py`
    - function `analyze_group(df_group, expertise_label, df_cat)` — Complete analysis pipeline for a single group (Experts or Novices): (behavioural/bh_fmri.py:641)
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri.py:481)
    - function `compute_strategy_colors_alphas(strategies)` — Assign a consistent color (first 5 unique → green, next 5 → red) and increasing alpha transparency (behavioural/bh_fmri.py:76)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri.py:457)
    - function `correlate_model_behavior(d_group, df_cat, expertise_label)` — For each categorical (and scalar) column in df_cat, compute: (behavioural/bh_fmri.py:583)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri.py:421)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri.py:400)
    - function `load_stimulus_categories(cat_path)` — Load stimulus categories from an Excel file and filter valid stim_id range. (behavioural/bh_fmri.py:569)
    - function `plot_choice_frequency(data, expertise_label)` — Create a bar plot showing how often each stimulus (by ID) was chosen in pairwise comparisons. (behavioural/bh_fmri.py:227)
    - function `plot_mds(dissimilarity_matrix, bold_title, expertise_label)` — Perform Multidimensional Scaling (MDS) on a precomputed dissimilarity matrix and plot the 2D embedding. (behavioural/bh_fmri.py:200)
    - function `plot_model_behavior_correlations(expert_results, novice_results, column_labels)` — Generate a side-by-side barplot of bootstrapped Pearson correlations (with 95% CI) (behavioural/bh_fmri.py:267)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Plot a representational dissimilarity matrix (RDM) as a square heatmap without inline colorbar. (behavioural/bh_fmri.py:137)
    - function `plot_shared_colorbar(cmap, vmin, vmax)` — Display a standalone colorbar for a diverging colormap (range vmin→vmax), (behavioural/bh_fmri.py:110)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri.py:505)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri.py:549)
  - `behavioural/bh_fmri_intercorr.py`
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri_intercorr.py:258)
    - function `compute_reliabilities_from_trials(trial_df_dict)` — Compute within-group and between-group RDM reliability from raw trial data (not precomputed RDMs). (behavioural/bh_fmri_intercorr.py:403)
    - function `compute_strategy_colors_alphas(strategies)` — Assign a consistent color (first 5 unique → green, next 5 → red) and increasing alpha transparency (behavioural/bh_fmri_intercorr.py:77)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri_intercorr.py:234)
    - function `correlate_model_behavior(d_group, df_cat, expertise_label)` — For each categorical (and scalar) column in df_cat, compute: (behavioural/bh_fmri_intercorr.py:346)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri_intercorr.py:198)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri_intercorr.py:177)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Plot a representational dissimilarity matrix (RDM) as a square heatmap without inline colorbar. (behavioural/bh_fmri_intercorr.py:111)
    - function `plot_reliability_bars(plot_data, out_fig, out_csv, run_id)` — Plot within- and between-group RDM reliability for Experts and Novices, (behavioural/bh_fmri_intercorr.py:484)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri_intercorr.py:282)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri_intercorr.py:326)
  - `behavioural/bh_fmri_splithalf.py`
    - function `_between_worker(args)` — Compute a split-half RDM similarity score between two groups using random half-sampling. (behavioural/bh_fmri_splithalf.py:70)
    - function `_split_half_worker(args)` — Compute a split-half RDM reliability score for a single group using a random split of subjects. (behavioural/bh_fmri_splithalf.py:26)
    - function `compute_directional_dsm(pairwise_df, expertise_label, do_plot)` — Build a directional preference DSM: DSM[i,j] = count(i over j) - count(j over i). (behavioural/bh_fmri_splithalf.py:350)
    - function `compute_reliabilities_from_trials(trial_df_dict, n_iter, random_seed)` — Estimate within- and between-group reliability of behavioral RDMs using split-half correlations. (behavioural/bh_fmri_splithalf.py:444)
    - function `compute_strategy_colors_alphas(strategies)` — Assign a consistent color (first 5 unique → green, next 5 → red) and increasing alpha transparency (behavioural/bh_fmri_splithalf.py:169)
    - function `compute_symmetric_rdm(pairwise_df, expertise_label, do_plot)` — Build a symmetric RDM: RDM[i,j] = | count(i preferred over j) - count(j over i) |. (behavioural/bh_fmri_splithalf.py:326)
    - function `create_pairwise_df(trial_df)` — Convert a subject's trial-level DataFrame into a pairwise comparison DataFrame. (behavioural/bh_fmri_splithalf.py:290)
    - function `load_participants(participants_xlsx_path, sourcedata_root)` — Load participant IDs and expertise statuses from Excel. (behavioural/bh_fmri_splithalf.py:269)
    - function `plot_rdm_heatmap(rdm, bold_title, expertise_label, colormap, vmin, vmax)` — Plot a representational dissimilarity matrix (RDM) as a square heatmap without inline colorbar. (behavioural/bh_fmri_splithalf.py:203)
    - function `plot_reliability_bars(plot_data, out_fig, out_csv, run_id)` — Plot within- and between-group RDM reliability for Experts and Novices, (behavioural/bh_fmri_splithalf.py:528)
    - function `process_single_participant(sub_id, is_expert, sourcedata_root, columns)` — Load, filter, and concatenate all 'exp' runs for a given participant. (behavioural/bh_fmri_splithalf.py:374)
    - function `worker_process(sub_info)` — Worker function for multiprocessing. Unpacks (sub_id, is_expert), processes the subject, (behavioural/bh_fmri_splithalf.py:418)
  - `behavioural/modules/chess.py`
    - function `check_accuracy(sub_resp, recommended_moves)` —  (behavioural/modules/chess.py:13)
    - function `clean_chess_move(move)` —  (behavioural/modules/chess.py:43)
    - function `convert_shorthand_to_long(fen, shorthand_moves, stim_id)` — Convert shorthand chess moves into from-cell-to-cell algebraic notation using a given FEN. (behavioural/modules/chess.py:22)
    - function `convert_single_shorthand(fen, shorthand_move, stim_id)` — Convert a shorthand chess move into from-cell-to-cell algebraic notation using a given FEN. (behavioural/modules/chess.py:51)
    - function `get_all_moves_with_int_eval(moves, evals)` —  (behavioural/modules/chess.py:17)
  - `behavioural/modules/helpers.py`
    - function `calculate_mean_and_ci(data)` —  (behavioural/modules/helpers.py:21)
    - function `load_and_sort_data(dataset_path, net_responses_path, humans_response_path)` — Load the datasets into Pandas DataFrames and sort them by 'stim_id'. (behavioural/modules/helpers.py:27)
- MATLAB Files: none found

## fmri_glm

- Python Modules:
  - `fmri_glm/roi_ttests_all.py`
    - function `compute_confidence_intervals(X1, X2)` — Compute mean difference, t-value, p-value, and 95% CI for each ROI. (fmri_glm/roi_ttests_all.py:586)
    - function `export_diff_stats_to_latex(df, label, output_path)` —  (fmri_glm/roi_ttests_all.py:521)
    - function `get_label_at_com(roi_val, glasser_data, affine_glasser, atlas_data_ho, affine_ho, atlas_labels_ho)` —  (fmri_glm/roi_ttests_all.py:596)
    - function `get_region_label(region_number)` —  (fmri_glm/roi_ttests_all.py:289)
    - function `paths_for(subject_ids, mode, data_dir, fname)` —  (fmri_glm/roi_ttests_all.py:498)
    - function `plot_glass_brain_from_df(df_input, atlas_img, title, cmap, base_font_size)` — Create and display a glass brain plot from a DataFrame with ROI and T_Diff values. (fmri_glm/roi_ttests_all.py:654)
    - function `plot_sig_rois_on_glasser_surface(sig_df, glasser_annot_files, fsaverage, title, threshold, cmap, output_file)` — Plot T_Diff values from a DataFrame on the flat cortical surface using Glasser parcellation (fmri_glm/roi_ttests_all.py:306)
    - function `run_analysis(DATA_DIR, CONTRASTS, atlas_path, mode)` —  (fmri_glm/roi_ttests_all.py:708)
    - function `save_and_open_plotly_figure(fig, title, outdir, png_out)` — Save a Plotly figure to HTML and optionally to PNG, then open the HTML in a browser. (fmri_glm/roi_ttests_all.py:451)
    - function `stars(p)` —  (fmri_glm/roi_ttests_all.py:295)
  - `fmri_glm/utils/plot_surf_glm.py`
    - function `plot_fsaverage_hemisphere(overlay, hemisphere, title, out_path)` — Plot a single hemisphere (left or right) in fsaverage space. (fmri_glm/utils/plot_surf_glm.py:16)
- MATLAB Files:
  - `fmri_glm/RSA_TwoSample_SecondLevel_MultiContrast.m`
  - `fmri_glm/TwoSample_SecondLevel_MultiContrast.m`
  - `fmri_glm/WithinGroup_SecondLevel_MultiContrast.m`
  - `fmri_glm/contrasts_only.m`
  - `fmri_glm/matlab_helpers/glm/adjust_contrasts.m`
  - `fmri_glm/matlab_helpers/glm/eventsBIDS2SPM.m`
  - `fmri_glm/matlab_helpers/glm/fMRIprepConfounds2SPM.m`
  - `fmri_glm/matlab_helpers/glm/findSubjectsFolders.m`
  - `fmri_glm/matlab_helpers/glm/gunzipNiftiFile.m`
  - `fmri_glm/matlab_helpers/glm/smoothNiftiFile.m`
  - `fmri_glm/matlab_helpers/run_subject_glm.m`
  - `fmri_glm/spm_glm_runallsubjects.m`
  - `fmri_glm/utils/BERT_spmGLMautoContrast_refactored_new-plots.m`

## manifold

- Python Modules:
  - `manifold/participation_ratio.py`
    - class `Config` —  (manifold/participation_ratio.py:29)
    - function `_assert_file_exists(path, label)` —  (manifold/participation_ratio.py:53)
    - function `_spm_dir_from_swd(spm_obj, fallback)` —  (manifold/participation_ratio.py:58)
    - function `compute_pr(roi_data)` —  (manifold/participation_ratio.py:104)
    - function `consolidate_results(expert_pr, novice_pr, roi_labels, stats_df, roi_name_map)` —  (manifold/participation_ratio.py:136)
    - function `get_spm_condition_betas(subject_id, cfg)` —  (manifold/participation_ratio.py:63)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois, cfg)` —  (manifold/participation_ratio.py:93)
    - function `main(cfg)` —  (manifold/participation_ratio.py:148)
    - function `per_roi_welch_and_fdr(expert_pr, novice_pr, roi_labels, alpha)` —  (manifold/participation_ratio.py:123)
    - function `process_subject(subject_id, atlas_data, unique_rois, cfg)` —  (manifold/participation_ratio.py:113)
  - `manifold/plot_pr_roi_size.py`
    - function `compute_entropy_pr(roi_data)` —  (manifold/plot_pr_roi_size.py:98)
    - function `get_spm_info(subject_id)` —  (manifold/plot_pr_roi_size.py:56)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois)` —  (manifold/plot_pr_roi_size.py:84)
    - function `main()` —  (manifold/plot_pr_roi_size.py:133)
    - function `plot_voxelcount_vs_pr(df, y_col, title_prefix, fname, out_dir)` —  (manifold/plot_pr_roi_size.py:118)
    - function `process_subject(subject_id, atlas_data, unique_rois)` —  (manifold/plot_pr_roi_size.py:108)
- MATLAB Files: none found

## mvpa

- Python Modules:
  - `mvpa/manifold.py`
    - function `compute_entropy_pr_per_roi(roi_data)` — Computes the participation ratio (PR) for a given ROI data matrix. (mvpa/manifold.py:262)
    - function `fdr_ttest(group1_vals, group2_vals, roi_labels, alpha)` — Performs independent two-sample t-tests for each ROI, then applies FDR correction. (mvpa/manifold.py:302)
    - function `get_spm_betas_info(subject_id)` — Returns, for each condition label (e.g. 'C1', 'C2', …), a list of dicts: (mvpa/manifold.py:78)
    - function `get_spm_info(subject_id)` — Loads the subject's SPM.mat structure and returns averaged beta images. (mvpa/manifold.py:126)
    - function `load_roi_voxel_data(subject_id, atlas_data, unique_rois)` — Loads and extracts ROI-level voxel data from averaged beta images for one subject. (mvpa/manifold.py:210)
    - function `main()` — Main analysis pipeline: (mvpa/manifold.py:601)
    - function `plot_all_rois_with_significance(stat_df, measure_name, group_means_df, expert_vals, nonexpert_vals, output_dir, use_fdr, sort_by, custom_colors)` — Plots ROI results for a given measure (e.g., Participation Ratio), (mvpa/manifold.py:356)
    - function `process_subject(subject_id, atlas_data, unique_rois)` — Pipeline for processing a single subject: (mvpa/manifold.py:557)
  - `mvpa/modules/helpers.py`
    - function `filter_significant_ROIs(sliced_dict, use_corrected_p_values, P_ALPHA, FDR_ALPHA)` — Updates the 'mean' column in the dataframes within the given dictionary based on thresholding rules (mvpa/modules/helpers.py:119)
    - function `load_all_data(root_path, expert_subjects, nonexpert_subjects, level)` — Load data for expert & non-expert subjects, concatenate into one DataFrame. (mvpa/modules/helpers.py:96)
    - function `load_subject_data(root_path, subject_list, is_expert, level)` — Load decoding accuracies from TSV files for a set of subjects, producing (mvpa/modules/helpers.py:22)
  - `mvpa/modules/roi_manager.py`
    - class `ROI` — Represents a single Region of Interest (ROI) using Python's dataclass. (mvpa/modules/roi_manager.py:72)
    - class `ROIManager` — Manages a collection of ROI objects, providing methods for (mvpa/modules/roi_manager.py:152)
  - `mvpa/mvpa_barplot.py`
    - function `format_contrast(s)` — Convert underscore ("_") to space and "vs" to hyphen ("-"), then capitalize. (mvpa/mvpa_barplot.py:33)
  - `mvpa/mvpa_barplot_comparison.py`
    - function `build_regressor_dataframe(regressor, analysis_results, roi_df, chance_value)` — Construct a long-format DataFrame for a single regressor, combining novices and experts stats, (mvpa/mvpa_barplot_comparison.py:151)
    - function `find_single_file(directory, pattern, file_description)` — Search for a single file matching the given glob pattern in the specified directory. (mvpa/mvpa_barplot_comparison.py:98)
    - function `format_contrast(s)` — Convert underscore ("_") to space and "vs" to hyphen ("-"), then capitalize. (mvpa/mvpa_barplot_comparison.py:35)
    - function `load_analysis_results(pickle_path)` — Unpickle and return the analysis results dictionary. (mvpa/mvpa_barplot_comparison.py:137)
    - function `load_roi_annotations(roi_annotation_path)` — Load ROI annotation data from a single TSV file inside the given directory. (mvpa/mvpa_barplot_comparison.py:122)
    - function `plot_expert_vs_novice(df, analysis_type, regressor_key, regressor_mapping, output_dir, chance_value, plot_nonsig_labels)` — Create and save a side-by-side bar plot comparing Experts vs Novices across ROIs, (mvpa/mvpa_barplot_comparison.py:265)
  - `mvpa/mvpa_surfplot_hcp.py`
    - function `build_significance_overlay(stats_df, manager)` — Constructs overlay maps for the left and right hemispheres based on statistical values. (mvpa/mvpa_surfplot_hcp.py:125)
    - function `format_contrast(s)` — Convert underscore ("_") to space and "vs" to hyphen ("-"), then capitalize. (mvpa/mvpa_surfplot_hcp.py:116)
    - function `load_glasser_surf()` —  (mvpa/mvpa_surfplot_hcp.py:63)
  - `mvpa/mvpa_ttest.py`
    - function `load_mvpa_subject(filepath)` — Loads a single subject's MVPA .tsv file and appends 'subject' + 'expert' metadata. (mvpa/mvpa_ttest.py:46)
    - function `organize_data_by_target_and_expertise(df)` — Organizes a flat dataframe into a nested dict: (mvpa/mvpa_ttest.py:17)
    - function `run_stats_on_df(df, method)` — Runs expert vs. nonexpert stats for all regressors in the given DataFrame. (mvpa/mvpa_ttest.py:87)
  - `mvpa/plot_surf_glasser.py`
    - function `load_freesurfer_lut(file_path)` — Load a FreeSurfer-style LUT file and create a Matplotlib colormap. (mvpa/plot_surf_glasser.py:16)
  - `mvpa/print_mvpa_stats.py`
    - function `extract_stats(result_dict, regressor)` —  (mvpa/print_mvpa_stats.py:55)
    - function `find_single_file(directory, pattern)` —  (mvpa/print_mvpa_stats.py:45)
    - function `load_results(pkl_path)` —  (mvpa/print_mvpa_stats.py:51)
    - function `print_latex_table(df, analysis, regressor, label)` —  (mvpa/print_mvpa_stats.py:76)
  - `mvpa/test_number_of_runs.py`
    - function `cohens_d_average_sd_unequal_var(x, y)` — Cohen's d using the average SD (often denoted d_av), recommended when variances differ. (mvpa/test_number_of_runs.py:180)
    - function `cohens_d_pooled_equal_var(x, y)` — Cohen's d using pooled SD (appropriate when variances are approximately equal). (mvpa/test_number_of_runs.py:150)
    - function `count_runs_from_spm(spm_path)` — Infer the number of runs for a subject from SPM.mat. (mvpa/test_number_of_runs.py:67)
    - function `hedges_g_from_d(d, nx, ny)` — Hedges' g bias correction applied to any Cohen's d-like estimator. (mvpa/test_number_of_runs.py:208)
    - function `main()` — Orchestrate data loading, testing, and reporting. (mvpa/test_number_of_runs.py:367)
    - function `save_run_count_latex_table(s_exp, s_nov, lev_stat, lev_p, test_name, t_stat, df_used, p_value, ci_low, ci_high, d_label, d_value, g_value, mw_stat, mw_p, output_dir, filename)` — Save a multi-column LaTeX table summarizing the analysis. (mvpa/test_number_of_runs.py:293)
    - function `save_run_count_report_txt(s_exp, s_nov, test_name, t_stat, df_used, p_value, ci_low, ci_high, d_label, d_value, g_value, lev_stat, lev_p, mw_stat, mw_p, output_dir, filename)` — Save a plain-text summary report of the analysis. (mvpa/test_number_of_runs.py:235)
    - function `summarize(arr)` — Compute basic NaN-safe descriptive stats for a 1D array. (mvpa/test_number_of_runs.py:122)
  - `mvpa/utils/anon_nii_filename.py`
    - function `format_sub_id(sub_id)` — Format the sub id to ensure it's in the format 'sub-xx'. (mvpa/utils/anon_nii_filename.py:74)
    - function `get_relative_path(path)` — Convert an absolute path to a relative path based on the current working directory. (mvpa/utils/anon_nii_filename.py:90)
    - function `main()` — Main function to handle command line arguments and control the flow of the script. (mvpa/utils/anon_nii_filename.py:137)
    - function `rename_files_in_directory(directory, confirm, dry_run)` — Collect and optionally rename files within a specified directory based on specific conditions. (mvpa/utils/anon_nii_filename.py:102)
  - `mvpa/utils/get_human_bh_rankings.py`
    - function `analyze_preferences(df_, sub, expertise, plot)` — Analyze preferences using a Bradley-Terry model and visualize the frequency of choices. (mvpa/utils/get_human_bh_rankings.py:306)
    - function `create_pairwise_data(df)` —  (mvpa/utils/get_human_bh_rankings.py:120)
    - function `determine_color_and_alpha(STRATEGIES)` —  (mvpa/utils/get_human_bh_rankings.py:68)
    - function `dsm_directional_preferences(df_, expertise, plot)` — Constructs and visualizes a directional preference matrix from pairwise comparison data, (mvpa/utils/get_human_bh_rankings.py:267)
    - function `plot_choice_frequency(data, subject, expertise)` — Generates a bar plot for the frequency of each choice made. (mvpa/utils/get_human_bh_rankings.py:337)
    - function `plot_mds(dissimilarity_matrix, title)` —  (mvpa/utils/get_human_bh_rankings.py:97)
    - function `plot_rdm(rdm, title, colormap)` — Plot a representational dissimilarity matrix (RDM) with external colored bars along the axes to group strategies. (mvpa/utils/get_human_bh_rankings.py:157)
    - function `rdm_sym(sub_df, expertise, plot)` — Constructs a dissimilarity matrix from pairwise comparison data, visualizes it, and returns the matrix. (mvpa/utils/get_human_bh_rankings.py:229)
  - `mvpa/utils/get_philips_MB_slicetiming.py`
    - function `calculate_slice_timing(tr, slices, multiband_factor)` — Calculate the slice timing order for fMRI data according to a specific interleaved pattern. (mvpa/utils/get_philips_MB_slicetiming.py:12)
    - function `extract_dicom_info(f)` — Extracts TR and the total number of slices from a DICOM file. (mvpa/utils/get_philips_MB_slicetiming.py:37)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py`
    - function `atoi(text)` —  (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:13)
    - function `calculate_stats(dfs, chance_levels, multiple_comp_corr)` — Extends the original function to include an option for multiple comparisons correction and (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:48)
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:159)
    - function `load_tsv_files(root_path, sub_list)` — Searches for all TSV files within subfolders of a specified root path and imports them. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:25)
    - function `natural_keys(text)` — Apply a natural sort algorithm that is case-insensitive. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:17)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:119)
    - function `save_dataframes(out_dir)` — Saves multiple pandas DataFrames to CSV files in the specified directory. The filenames are based on the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa.py:283)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py`
    - function `calculate_stats(dfs, chance_levels, multiple_comp_corr)` — Extends the original function to include an option for multiple comparisons correction and (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py:45)
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py:160)
    - function `group_rois_in_dfs(dfs_list)` — Groups Regions of Interest (ROIs) in each DataFrame within a list according to (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py:283)
    - function `load_tsv_files(root_path, sub_list)` — Searches for all TSV files within subfolders of a specified root path and imports them. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py:22)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roigroups.py:117)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py`
    - function `calculate_stats(dfs, chance_levels, multiple_comp_corr)` — Extends the original function to include an option for multiple comparisons correction and (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py:52)
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py:167)
    - function `group_rois_in_dfs(dfs_list)` — Groups Regions of Interest (ROIs) in each DataFrame within a list according to (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py:290)
    - function `load_tsv_files(root_path, sub_list)` — Searches for all TSV files within subfolders of a specified root path and imports them. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py:29)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_mvpa_roilobes.py:124)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm.py`
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm.py:55)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm.py:15)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roigroups.py`
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roigroups.py:72)
    - function `group_rois_in_dfs(dfs_list)` — Groups Regions of Interest (ROIs) in each DataFrame within a list according to (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roigroups.py:198)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roigroups.py:29)
  - `mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roilobes.py`
    - function `color_significant_parcels_on_flat(freesurfer_path, lh_annotation, rh_annotation, averages_df, p_values_df, contrasts, out_path, significance_level)` — Create brain images with significant parcels colored on a flattened surface using FreeSurfer annotation files. (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roilobes.py:73)
    - function `group_rois_in_dfs(dfs_list)` — Groups Regions of Interest (ROIs) in each DataFrame within a list according to (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roilobes.py:199)
    - function `reverse_roi_name_to_annotation_label(roi_name, lh_labels_list, rh_labels_list)` — Given a volume label, finds the corresponding label name in the sorted list for the (mvpa/utils/plot_HCPMM1_results/plot_HCP_surface_rsaglm_roilobes.py:30)
  - `mvpa/utils/plot_HCPMM1_results/plot_model_data.py`
    - function `invert_name(name)` — Converts a given column name to match the corresponding region name in coord_df. (mvpa/utils/plot_HCPMM1_results/plot_model_data.py:121)
    - function `plot_bars(df, title, labels, confidence_intervals, significance, save_to)` —  (mvpa/utils/plot_HCPMM1_results/plot_model_data.py:83)
    - function `plot_curves(df, title, labels, save_to, confidence_intervals)` — Plots curves from a DataFrame and adds specified confidence interval shading, (mvpa/utils/plot_HCPMM1_results/plot_model_data.py:24)
  - `mvpa/utils/plot_HCP_parcels.py`
    - function `run_python_scripts(directory)` —  (mvpa/utils/plot_HCP_parcels.py:13)
  - `mvpa/utils/sanitize_json.py`
    - function `find_nan_json_files(folder)` — Scan for JSON files in the directory that contain non-compliant float values. (mvpa/utils/sanitize_json.py:51)
    - function `is_nan(value)` — Check if a value is NaN. (mvpa/utils/sanitize_json.py:16)
    - function `main(folder, dry_run)` —  (mvpa/utils/sanitize_json.py:72)
    - function `process_file(file_path, dry_run)` — Process a single JSON file, replacing NaN values and optionally printing changes. (mvpa/utils/sanitize_json.py:20)
  - `mvpa/utils/sf_eval.py`
    - function `compute_and_plot_rdm(dataframe, columns, title)` —  (mvpa/utils/sf_eval.py:161)
    - function `compute_king_safety(fen)` —  (mvpa/utils/sf_eval.py:106)
    - function `compute_material_imbalance(fen)` —  (mvpa/utils/sf_eval.py:99)
    - function `compute_punishability(fen, engine, threshold)` —  (mvpa/utils/sf_eval.py:84)
    - function `compute_tactical_pressure(fen)` —  (mvpa/utils/sf_eval.py:44)
    - function `compute_visual_rdm(filenames)` —  (mvpa/utils/sf_eval.py:36)
    - function `detect_motifs(fen)` —  (mvpa/utils/sf_eval.py:63)
    - function `evaluate_expectation(fen, engine)` —  (mvpa/utils/sf_eval.py:27)
    - function `extract_features(args)` —  (mvpa/utils/sf_eval.py:122)
    - function `plot_rdm(matrix, title, label)` —  (mvpa/utils/sf_eval.py:115)
  - `mvpa/utils/svm_class_displacement.py`
    - function `load_eye_tracking_data(root_dir, participants)` — Load and preprocess eye tracking data from the specified directory for the given participants. (mvpa/utils/svm_class_displacement.py:50)
    - function `main(eye_tracking_data)` — Main function to run the SVM decoding analysis. (mvpa/utils/svm_class_displacement.py:330)
    - function `plot_results(results)` — Create and display publication-ready visualizations of the SVM decoding results. (mvpa/utils/svm_class_displacement.py:220)
    - function `prepare_features(eye_tracking_data)` — Prepare features for SVM classification from eye-tracking data. (mvpa/utils/svm_class_displacement.py:103)
    - function `run_svm_decoding(X, y, groups)` — Perform SVM decoding analysis using stratified group k-fold cross-validation. (mvpa/utils/svm_class_displacement.py:139)
  - `mvpa/utils/svm_class_xy.py`
    - function `load_eye_tracking_data(root_dir, participants)` — Load and preprocess eye tracking data from the specified directory for the given participants. (mvpa/utils/svm_class_xy.py:42)
    - function `main(eye_tracking_data)` — Main function to run the SVM decoding analysis. (mvpa/utils/svm_class_xy.py:322)
    - function `plot_results(results)` — Create and display publication-ready visualizations of the SVM decoding results. (mvpa/utils/svm_class_xy.py:212)
    - function `prepare_features(eye_tracking_data)` — Prepare features for SVM classification from eye-tracking data. (mvpa/utils/svm_class_xy.py:95)
    - function `run_svm_decoding(X, y, groups)` — Perform SVM decoding analysis using stratified group k-fold cross-validation. (mvpa/utils/svm_class_xy.py:131)
- MATLAB Files:
  - `mvpa/matlab_helpers/createTimestamp.m`
  - `mvpa/matlab_helpers/expand_fmriprep_surfaces.m`
  - `mvpa/matlab_helpers/generate_con_img.m`
  - `mvpa/matlab_helpers/mvpa/ROI.m`
  - `mvpa/matlab_helpers/mvpa/ROIManager.m`
  - `mvpa/matlab_helpers/mvpa/getHierarchicalItems.m`
  - `mvpa/matlab_helpers/mvpa/loadAndIntersectROI.m`
  - `mvpa/matlab_helpers/mvpa/loadColorTable.m`
  - `mvpa/matlab_helpers/mvpa/performMVPA.m`
  - `mvpa/matlab_helpers/mvpa/performMVPA_matlabsvm.m`
  - `mvpa/matlab_helpers/mvpa/performRSACorrelation.m`
  - `mvpa/matlab_helpers/mvpa/performRSARegression.m`
  - `mvpa/matlab_helpers/mvpa/prepareRoiFile.m`
  - `mvpa/matlab_helpers/mvpa/sliceDatasetByHierarchy.m`
  - `mvpa/matlab_helpers/saveCurrentScript.m`
  - `mvpa/mvpa_rsa_all_regressors.m`
  - `mvpa/rsa_searchlight_final.m`
  - `mvpa/utils/make_sphere_rois.m`
  - `mvpa/utils/test_roi_manager.m`

## neurosynth

- Python Modules:
  - `neurosynth/modules/config.py`
    - function `setup_logging(log_file, level)` — Configure and return a logger with a consistent format. (neurosynth/modules/config.py:20)
  - `neurosynth/modules/io_utils.py`
    - function `load_nifti(path)` — Load an image file using nilearn and return the NIfTI image object. (neurosynth/modules/io_utils.py:23)
    - function `load_term_maps(map_dir)` — Return a dictionary mapping each term name to its NIfTI file. (neurosynth/modules/io_utils.py:9)
  - `neurosynth/modules/plot_utils.py`
    - function `plot_correlations(df_pos, df_neg, df_diff, out_fig, out_csv, run_id)` — Paired bar plot (POS vs NEG). (neurosynth/modules/plot_utils.py:134)
    - function `plot_difference(diff_df, out_fig, run_id, col_pos, col_neg)` — Plot bar chart of correlation differences (pos - neg). (neurosynth/modules/plot_utils.py:270)
    - function `plot_map(arr, ref_img, title, outpath, thresh)` — Render and save a glass brain plot of a 3D array. (neurosynth/modules/plot_utils.py:48)
    - function `plot_term_maps(term_maps, out_dir)` — Plot and save brain maps for each term in term_maps. (neurosynth/modules/plot_utils.py:110)
  - `neurosynth/modules/stats_utils.py`
    - function `compute_all_zmap_correlations(z_pos, z_neg, term_maps, ref_img, n_boot, fdr_alpha, ci_alpha, random_state, n_jobs)` — Compute correlation statistics for positive and negative z-maps against (neurosynth/modules/stats_utils.py:159)
    - function `generate_latex_multicolumn_table(data_dict, output_path, table_type, caption, label)` — Generate and save a LaTeX multicolumn table from multiple regressors. (neurosynth/modules/stats_utils.py:388)
    - function `get_brain_mask(ref)` — Loads the ICBM152 GM brain mask and resamples it to match the reference image. (neurosynth/modules/stats_utils.py:23)
    - function `remove_useless_data(data, brain_mask_flat)` — Remove problematic voxels from stacked maps. (neurosynth/modules/stats_utils.py:104)
    - function `save_latex_correlation_tables(df_pos, df_neg, diff_df, run_id, out_dir)` — Save LaTeX tables for positive/negative/difference z-map correlations. (neurosynth/modules/stats_utils.py:302)
    - function `split_and_convert_t_to_z(t_map, dof)` — Take a 3D t-map and produce two 3D z-maps: (neurosynth/modules/stats_utils.py:53)
- MATLAB Files: none found

## rois

- Python Modules:
  - `rois/make_glasser_rois.py`
    - function `assign_color(cortex_id)` —  (rois/make_glasser_rois.py:91)
    - function `parse_afni_value_label_dtable(head_path)` —  (rois/make_glasser_rois.py:8)
    - function `regionName_to_cifti(region_name)` —  (rois/make_glasser_rois.py:44)
  - `rois/mke_bilalic_rois.py`
    - function `sphere_mask_in_3d(center_mni, radius_mm, out_shape)` — Return a 3D boolean array of shape=out_shape indicating which voxels (rois/mke_bilalic_rois.py:155)
- MATLAB Files: none found
